apiVersion: v1
kind: Service
metadata:
  name: corex-ai-gateway
  namespace: corex
  labels:
    app: corex-ai-gateway
    app.kubernetes.io/name: corex-ai-gateway
    app.kubernetes.io/component: gateway
    app.kubernetes.io/part-of: corex-platform
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: corex-ai-gateway
